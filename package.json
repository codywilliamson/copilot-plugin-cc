{
  "name": "@codywilliamson/copilot-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A Claude Code plugin for GitHub Copilot, built to bring codex-plugin-cc-style workflows to Copilot too.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "bump-version": "node scripts/bump-version.mjs",
    "check-version": "node scripts/bump-version.mjs --check",
    "test": "node --test tests/*.test.mjs"
  }
}
